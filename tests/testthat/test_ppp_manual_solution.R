context("ppp_manual_solution")

test_that("single solution, no constraints", {
})

test_that("single solution, locked in + out constraints", {
})

test_that("multiple solutions, no constraints", {
})

test_that("multiple solutions, locked in + out constraints", {
})

test_that("invalid arguments", {
})
